# Plan to Fix Remaining UI Build Errors

The following final adjustments will resolve the remaining TypeScript errors in the frontend:

## 1. `src/App.tsx`
- **Issue**: Attempting to import `clearModelCatalogCache` and `getModelCatalog` from a deleted AI models module (`./lib/ai/modelCatalogClient`).
- **Fix**: Remove the import statement entirely. In the `useEffect` that attempts to fetch the catalog, replace the `getModelCatalog(...)` call with `Promise.resolve()` to completely neuter the API request logic without breaking the application loading sequence.

## 2. `src/components/settings/SettingsDrawer.tsx`
- **Issue**: The `ModelSelection` component is receiving `surface` and `route` properties, but since it has been mocked out as a generic empty component, these props violate the type signature.
- **Fix**: Remove the `surface="drawer" route={route}` props from the `<ModelSelection>` rendering block.

## 3. `src/features/projects/lib/devServerAiRecovery.ts`
- **Issue**: `AI_BASE_URL` is imported from the deleted `@/lib/ai/apiEndpoints` file.
- **Fix**: Remove the import. Since the `inferDevServerRecovery` function already explicitly returns `null` at the top of its block (bypassing the logic anyway), the subsequent unreachable `fetch` call that utilizes `AI_BASE_URL` should be entirely deleted.

Once these files are cleaned up via simple regex edits or targeted replacements, `npm run typecheck` and `npm run build` will execute flawlessly.