# Main synchronization — 2026-09-04

Status: integrated

The repository-architecture branch incorporates `main` through commit `ea679997d0d89e6eafb15aa2932313198aafe891` (release 0.2.1) using a two-parent merge commit. Upstream history is therefore recorded as merged rather than replayed as unrelated copies.

## Path-aware mappings

- `features/projects/browser/BrowserPreviewActions.tsx` → `features/browser/BrowserPreviewActions.tsx`
- project workbench components → `features/workbench/`
- project changes UI/page → `features/source-control/`
- `ProjectSidebar` and sidebar helpers → `features/projects/ui/`
- application sidebar shell → `app/shell/sidebar/`
- App Store page/model → `features/devapps/pages/` and `features/devapps/model/`
- project settings and personal settings updates → `features/settings/`
- project header state → `features/projects/model/`

The retired App Store sidebar and catalog were removed from both their historical and feature-owned locations. Compatibility facades remain only where active imports or route assembly still depend on an old public path.

## Upstream changes incorporated

- release 0.2.1 and unsigned macOS release handling;
- native context menus, Finder/external-editor support, sidebar and title-bar polish;
- unified DevApps Store presentation;
- per-organization DevApp runtime image repositories;
- clearer contained-runtime and publishing failures;
- Cloudflare, Convex, native-container, shared IPC, and desktop API contract updates.

## Validation boundary

The connector environment can inspect and write Git objects but cannot execute the Electron checkout. Source-based tests were remapped to canonical feature paths. Local typecheck, Vitest, production build, and GUI smoke testing remain required before merge to `main`.
