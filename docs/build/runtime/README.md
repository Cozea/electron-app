Runtime metadata lives here.

Expected layout:

- `capability-catalog.json`
- `capability-catalog.sig`
- `runtime-public-key.pem`

Use scripts:

- `bun run prepare:runtime-metadata`
- `bun run sign:runtime-metadata`
