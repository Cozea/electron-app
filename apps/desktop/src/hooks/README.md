# Shared renderer hooks

This directory contains only domain-neutral renderer hooks and temporary compatibility facades.

Feature-specific hooks belong under `features/<capability>/hooks`; application-wide orchestration belongs under `app/hooks`. Do not add new domain hooks here.
