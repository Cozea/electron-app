# Native preview feature

Owns renderer-side native simulator preview surfaces and native-preview presentation types.

The workbench may host these views, while native-preview behavior remains feature-owned. New imports should use `@/features/native-preview/...`.
