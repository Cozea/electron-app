# Terminal feature

Owns renderer-side terminal views, keep-alive behavior, and workbench-session terminal binding.

The workbench embeds terminal views through adapters. New imports should use `@/features/terminal/...`.
