# Assistant model

Renderer-side assistant state, orchestration projection, transport, provider model rules, thread details, and prompt-bound terminal context live here.

Persisted keys and public exports remain unchanged. The former `src/stores` compatibility facades have been removed; import from `@/features/assistant/model/...`.
