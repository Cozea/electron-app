# Assistant model

Renderer-side assistant state, orchestration projection, transport, provider model rules, thread details, and prompt-bound terminal context live here.

Persisted keys and public exports remain unchanged during migration. Historical modules under `src/stores` are compatibility facades and must not receive new behavior.
