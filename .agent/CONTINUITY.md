# CONTINUITY

## [PLANS]
- 2026-08-27T21:50Z [USER] Apply all project-switch redraw levers: hover prefetch, startTransition navigation, workbench keep-alive, xterm pooling, phase marks.
- 2026-08-26T17:36Z [USER] Implement free dockview OSS policies except Changes edge groups (#1) and header-slot migration (#4): group locks, size constraints, thin activity mode, tab-group polish, basic tab overflow.
- 2026-08-26T17:03Z [USER] Dependency upgrade swarm after #70: phased dockview + parallel safe bumps.
  - Wave 1 PRs (parallel, isolated worktrees): (1) workbench hygiene + dockview 6.0.5→6.6.1 (2) electron → latest 40.x (3) Lexical → 0.49 (4) claude-agent-sdk → 0.3.x
  - Wave 2: dockview → 7 via `dockview-react` (draft PR); no enterprise purchase without explicit [USER] approval
  - Hold: Vite 8 (electron-vite peers), TypeScript 7, dockview-enterprise features (auto-hide, compass, layout history, pinned tabs)

## [DECISIONS]
- 2026-08-26T22:58Z [USER] Tile rearrange DnD still broken; predates keep-alive. [CODE] Dockview default `dndStrategy:"auto"` uses HTML5 mouse drag; Electron webview often never fires `dragstart` (same finding as cloud VM smoke). Set `dndStrategy:"pointer"` on `DockviewReact`. Drag handle is the tab chip, not the tile body; native BrowserViews still eat drops over preview content.
- 2026-08-26T21:55Z [CODE] Keep last 3 workbench sessions mounted (current + 2). Matches warm runtime host budget.
- 2026-08-26T17:03Z [USER]+[CODE] Stay on dockview OSS MIT path through v7/v8 free APIs; enterprise only after license decision.
- 2026-08-26T17:03Z [ASSUMPTION] Merge order: hygiene+dockview6.6 first, then electron/Lexical/SDK in any order; dockview v7 after 6.6 lands or stacked on it.
- 2026-08-26T17:25Z [USER] Exclude unused Claude SDK platform native binaries from the electron-builder app bundle on #76.

## [PROGRESS]
- 2026-08-26T23:10Z [USER] Infinite loader next to branch selector returned after #99. [CODE] Restored bounded collab reconnect in `connectionStatusModel` + compact indicator: 15s cap, session idle/error/encryption states, compact `.loader` only for `spin` (not pulse). typecheck + 13 model tests green.
- 2026-08-26T21:55Z [CODE] Project-switch path: hover prefetch (Convex + workspace IPC + lane), `startTransition` navigation, keep project shell mounted (no full-tree "Loading project..."), workbench keep-alive host, phase marks (`navigate` → `first-tile-paint`). typecheck + oxlint + 9 new unit tests green. Electron UI not smoked in this turn.
- 2026-08-26T17:03Z [TOOL] Swarm Wave 1 launched (4 agents).
- 2026-08-26T17:12Z [TOOL] Wave 1 item (1) shipped: [PR #71](https://github.com/Cozea/electron-app/pull/71) `chore/workbench-dockview-6.6` @ 12bb581a. typecheck + oxlint + electron-vite build pass.
- 2026-08-26T17:15Z [CODE] Discarded redundant uncommitted copies of ProjectLayout + useWorkbenchDockviewRuntime on main (now live only on #71).
- 2026-08-26T17:15Z [TOOL] Wave 2 launched: dockview v7 draft stacked on `chore/workbench-dockview-6.6`.
- 2026-08-26T17:13Z [TOOL] Wave 1 item (2) shipped: [PR #72](https://github.com/Cozea/electron-app/pull/72) electron 40.8.5→40.10.6 @ c8b2f1d0. typecheck/lint/build/vitest green. Chromium unchanged; Node 24.14→24.15; ABI 143 unchanged.
- 2026-08-26T17:13Z [TOOL] Wave 1 item (3) shipped: [PR #73](https://github.com/Cozea/electron-app/pull/73) Lexical 0.42.0→0.49.0 (12 packages). package.json+lock only; typecheck/lint/vitest green. Manual composer smoke still needed.
- 2026-08-26T17:24Z [TOOL] Wave 1 item (3) shipped: [PR #73](https://github.com/Cozea/electron-app/pull/73) `chore/lexical-0.49` @ 2a4f3656. lexical + 11 `@lexical/*` 0.42.0→0.49.0; package.json + bun.lock only, zero source changes. typecheck + oxlint + full vitest (1172 pass) green.
- 2026-08-26T17:16Z [TOOL] Wave 1 item (2) shipped: [PR #72](https://github.com/Cozea/electron-app/pull/72) `chore/electron-40.10` @ c8b2f1d0. electron 40.8.5 → 40.10.6. All three typechecks + oxlint + electron-vite build + full vitest (1172 pass / 5 skip / 0 fail) + both electron audits pass. No packaged `.app` smoke test.
- 2026-08-26T17:34Z [TOOL] Wave 2 shipped: draft [PR #75](https://github.com/Cozea/electron-app/pull/75) `chore/dockview-v7` @ d4626cd0, base `chore/workbench-dockview-6.6` (stacked on #71, worktree `.worktrees/dockview-v7`). `dockview@6.6.1` → `dockview-react@7.0.4`; 15 files. typecheck + oxlint + electron-vite build green. Runtime behavior unexercised — layout deserialization, floating/popout groups, Changes panel drop-suppression, and tab groups listed as human-verify risks in the PR body.

- 2026-08-26T17:22Z [TOOL] Wave 1 item (4) shipped: draft [PR #76](https://github.com/Cozea/electron-app/pull/76) `chore/claude-agent-sdk-0.3` @ f96dfa93 (worktree `.worktrees/claude-sdk`). `@anthropic-ai/claude-agent-sdk` 0.2.89 → 0.3.246. All three typechecks + oxlint + full vitest (1172 pass) green. Draft pending a packaging decision, not unfinished code.
- 2026-08-26T17:25Z [CODE] #76 follow-up @ 2e208f9c: `files` negation `!**/node_modules/@anthropic-ai/claude-agent-sdk-*/**` + dropped those pkgs from `mac.x64ArchFiles`. PR marked ready for review.
- 2026-08-26T17:40Z [CODE] Free OSS dockview policies on main checkout (uncommitted): `applyWorkbenchDockviewPolicies` — group locks for Agent/Dev Server, raised runtime min sizes + re-apply after hydrate, auto tab groups, overflow list explicit on, thinned activity-mode hook. Skipped Changes edge groups + header-slot migration per [USER].
- 2026-08-27T00:00Z [CODE] Post-#77 local revision (uncommitted): hide tab-group underline CSS, `tabGroupIndicator: "none"`, remove auto tab-group preset + `applyWorkbenchGroupLocks` (restores tile drag), add `dissolveOrphanPresetTabGroups()` to unlock/clean leftover preset chips.
- 2026-08-27T00:00Z [TOOL] Dev server restart after abort: `bun run dev` green @ http://localhost:5183/, Electron boot OK.
- 2026-08-27T19:30Z [TOOL] Cloud VM DnD smoke (bc-f27a863f): fix applied, all groups `locked:false`. Agent→Browser pointer drag PASS — drop overlays (`.dv-drop-target-*`, `.dv-tab--dragging`) visible, tab-stack layout changed. No dockview DnD console errors. Linux automation needed `dndStrategy:"pointer"`; xdotool/html5 mouse did not fire `dragstart`.

- 2026-08-27T21:15Z [USER] Swarm: branch/sync honesty fixes (4 parallel agents): (1) branch pill no-git label + ahead/behind no-remote + stale branch (2) collab sync indicator states + shared mode copy (3) fix missing server workspace (4) unit tests for branch display helpers.
- 2026-08-27T21:20Z [TOOL] Swarm item (4) shipped: [PR #96](https://github.com/Cozea/electron-app/pull/96) `test/workbench-branch-sync-honesty` @ e437df65. Extracted `workbenchBranchDisplay.ts` helpers + 31 vitest cases; typecheck green.
- 2026-08-27T21:20Z [TOOL] Swarm item (1) shipped: [PR #94](https://github.com/Cozea/electron-app/pull/94) `fix/workbench-branch-git-honesty` @ 52f9764e — No git label, (no remote) ahead/behind, unverified `?` tooltip. typecheck + oxlint green, mergeable.
- 2026-08-27T21:25Z [TOOL] Swarm item (4) shipped: [PR #96](https://github.com/Cozea/electron-app/pull/96) `test/workbench-branch-sync-honesty` — `workbenchBranchDisplay.ts` + 31 unit tests; hook wiring deferred to #94. mergeable. Overlaps #94/#95 on branch-control files.

## [DISCOVERIES]
- 2026-08-26T23:45Z [USER] Not a deferred v1: the asking person will put this in a real org. Org DevApps = built artifact in Cozea (no localhost, no Dev Server registration, no source for consumers). Settings must include org controls: which orgs you belong to, and which org DevApps you can use. Settings today is personal-device only (Account / Appearance / Tooling) — an org surface has to be added.
- 2026-08-26T23:41Z [USER] Org DevApps must not be localhost and must not register in the Dev Server / localhost preview path. Open as an in-Cozea WebContentsView on a custom protocol serving a **built** artifact. Publish = build+catalog; never `startDevServerRun` / `ensureSession` on source for consumers.
- 2026-08-26T23:39Z [USER] Org DevApps must not be "everyone runs the source on localhost." Consumers should not get source. The mini app should run **inside Cozea** as a tile (packaged/built artifact or hosted preview), not `npm run dev` against a shared checkout.
- 2026-08-26T23:37Z [USER] Left-nav control next to the project name is **publish to the org store**, not "run the app." Opening the published mini app is the same path as any other DevApp (Store / launcher tile).
- 2026-08-26T23:31Z [USER] Refined DevApp vision: one org member publishes a project into the Cozea DevApp store; store has an org-private section; those mini apps are used like built-in tiles (open from any workbench, first-class dock tiles). Current "Local DevApps / This Mac" is the same shelf shape with machine-local scope. Org catalog + independent tiles (not Dev Server singleton) are the gap.
- 2026-08-26T23:28Z [USER] Erick vision for Project DevApps: org-wide launch from left-nav (whole org uses the vibe-coded app); non-org share like collab invites; must open inside other projects. Pitch to investor can be working-not-polished. Known gap he named: they open as localhost via the Dev Server singleton, so they block other local hosts and a new browser/dev-server tile shows the latest running app. Hosted preview for testers was discussed as a later share path, not what shipped. [CODE] Shipped (PR #70 on main) is a machine-local launch recipe (`This Mac` catalog, `Launch as DevApp` overflow), not org/invite/hosting. Cross-project open is real for the tile (destination dock, source workspace/process). Singleton `tileType:"devServer"` is the collision Erick described.
- 2026-08-26T23:10Z [CODE] PR #99 `resolveConnectionStatusPresentation` maps shared-mode + `!collabConnected` to unbounded `motion:"spin"` / "Collab Reconnecting". Compact chrome treats spin (and previously pulse) as `.loader`. #95 had `RECONNECT_CAP_MS` + session/encryption branches; #99 dropped them. Local-only / no-gateway projects never connect Yjs, so the header spinner never stops.
- 2026-08-26T21:55Z [CODE] xterm pooling already shipped: `TerminalViewHost` + `KeepAliveTerminalView` reparent mounts (max 8 detached). Workbench CSS keep-alive avoids even that park on switch-back because tiles stay mounted.
- 2026-08-26T22:40Z [CODE] Returning to a workbench reset splits to a row of columns: tile *set* is in zustand, split *tree* is dockview JSON. Hydrate did `api.clear()` + `buildDefaultDockview` (every tile `direction:"right"`). Keep-alive hide/`clip-path` could also serialize a degenerate grid. Fix: never clear a live dock (`totalPanels > 0`), persist on deactivate, ignore layout saves while hidden, drop `clip-path`, opaque active slot.
- 2026-08-26T21:55Z [CODE] React 19.2.4 exports `Activity`, but `mode="hidden"` destroys effects. Wrong primitive for dockview/xterm. CSS hide keeps widget size and effects.
- 2026-08-26T17:22Z [TOOL] claude-agent-sdk 0.3 is additive for us: the only removed exports are the `unstable_v2_*` session API we never used, and every `Options` key + `Query` method the adapter calls survives. Evidence: stripping `@ts-nocheck` from ClaudeAdapter/ClaudeProvider and typechecking against 0.2.89 vs 0.3.246 yields byte-identical error sets (55 pre-existing effect-smol/branded-type errors). That strip-and-diff trick is the only way to verify SDK surface changes in this repo — a green `bun run typecheck` proves nothing while those files are nocheck.
- 2026-08-26T17:22Z [CODE] 0.3 adds ~15 SDK message variants (14 new `type: "system"` subtypes + top-level `conversation_reset`). ClaudeAdapter's default branch converted every unknown subtype into a `runtime.warning`, which renders as a chat work entry — so the bump alone would have spammed the timeline with `thinking_tokens`/`control_request_progress` warnings every turn. Fixed with a silent-subtype set plus real text for the six diagnostic ones. Codex/OpenCode adapters have the same unknown-event-to-warning shape; check them on their next SDK bumps.
- 2026-08-26T17:22Z [CODE] 0.3 packaging is a different shape: vendored `cli.js` + `vendor/` (ripgrep, audio-capture) are gone, replaced by per-platform native binaries as optional deps — 47 MB package becomes 4.6 MB + a 230 MB `claude-agent-sdk-darwin-arm64` binary that electron-builder will copy into the app. Cozea never uses it (both call sites always pass `pathToClaudeCodeExecutable`, default `claude` from PATH), so it is dead weight; the `files` negation is proposed but unverified in #76. `sharp` also left the tree entirely (old-SDK transitive optional dep, unimported here).
- 2026-08-26T17:22Z [CODE] 0.3 `env` REPLACES the subprocess environment instead of merging with `process.env`. We are safe only because `mergeProviderInstanceEnvironment` already spreads `process.env` — do not "simplify" that call to pass just the provider overrides.
- 2026-08-26T17:12Z [CODE] dockview 6.6.1 has a breaking type change *within* 6.x: drop `nativeEvent` widened to `DragEvent | PointerEvent` (pointer-based dragging). Evidence: `TS2339: Property 'dataTransfer' does not exist on type 'DragEvent | PointerEvent'` at useWorkbenchDockviewRuntime.ts:729,742. Fixed via a `"dataTransfer" in e` narrowing helper, not a cast. Expect the same call sites to need attention on the v7 bump.
- 2026-08-26T17:12Z [CODE] dockview 6.6.1 keeps `dockview/dist/styles/dockview.css` and the `dv-` class prefix, so `workbench.css` theme overrides needed no change.
- 2026-08-26T17:16Z [TOOL] electron 40.8.5 → 40.10.6 is Chromium-neutral: both ship Chromium 144.0.7559.236; only Node moves (24.14.0 → 24.15.0). Node ABI stays 143, so `node-pty` prebuilds and `@cozea/pty` napi `.node` binaries need no rebuild. Evidence: `node-abi` getAbi → 143 for both; `electron --version` → v40.10.6.
- 2026-08-26T17:16Z [CODE] The 40.9.3 CORS tightening for `supportFetchAPI` schemes and the 40.10.6 `ProtocolResponse.url` session-routing fix are non-issues here — no `registerSchemesAsPrivileged` / `protocol.handle` / `registerFileProtocol` call sites exist under `electron/`.
- 2026-08-26T17:16Z [CODE] `electron-mksnapshot` stays pinned at `^38.8.2`, two majors behind. Pre-existing skew, only reachable via the opt-in `build:snapshot` script (not `build`/`dist`); left alone deliberately on #72.
- 2026-08-26T17:16Z [TOOL] Sandboxed shells break two unrelated things in this repo: `git worktree add` (cannot write `.git/config`) and the GitManager vitest suite (`git init` fails on `.git/hooks/` in /tmp, 122 false failures). Both are clean when run unsandboxed — do not chase these as real regressions.
- 2026-08-26T17:24Z [CODE] Lexical 0.42→0.49 needed no composer changes. The 0.49 `$config()` migration (#8640) does NOT break custom nodes using legacy `static getType()/clone()/importJSON()`: `getStaticNodeConfig` only derives type from a *user-defined own* static `getType` (synthesized ones are brand-skipped to avoid #8867 recursion). The 0.45 "remove empty inline elements" transform is registered only for `klass.prototype instanceof ElementNode` with an overridden `isInline`, so the `TextNode`/`DecoratorNode` composer chips are ineligible. Evidence: headless 0.49 smoke test of the three composer node classes passed 14/14 (registration, token mode, inline decorator, editor-state JSON round-trip).
- 2026-08-26T17:24Z [CODE] `@lexical/react@0.49` no longer pulls `react-error-boundary` (dropped from bun.lock); nothing in-repo imports it directly. New transitive deps: `@lexical/{a11y,extension,internal}`. Watch out: `oxfmt` is coincidentally also `^0.42.0` — do not sweep it with lexical version bumps.
- 2026-08-26T17:24Z [TOOL] Sandboxed `bun run test` reports ~122 bogus failures because suites `git init` into `/tmp` ("Operation not permitted"); rerun unsandboxed before believing a red suite.
- 2026-08-26T17:34Z [CODE] dockview v7 = package realignment, not an API rewrite: `dockview` becomes a `dockview-core` re-export and React bindings move to `dockview-react`, which re-exports everything — so all 12 call sites (components *and* type-only) collapse onto one specifier, and the stylesheet is byte-identical with the `dv-` prefix intact. Only two real API breaks touched us: `onUnhandledDragOverEvent` → `onUnhandledDragOver` (loud, typecheck catches it) and `onDidActivePanelChange` now emitting `{ panel, origin }` instead of the panel (**silent** — the old handler reads `undefined` off the event object and quietly stops tracking the active tile). `rootOverlayModel` removal and the other v7 renames are unused here.
- 2026-08-26T17:34Z [CODE] The 6.6.1 `DragEvent | PointerEvent` landmine survives v7 unchanged (`dockview-core/dist/cjs/dnd/droptarget.d.ts`), so the `"dataTransfer" in e` narrowing helper stays correct as-is through the bump.
- 2026-08-26T17:34Z [CODE] Package renames silently break `rendererManualChunks` in `electron.vite.config.ts`: the `/node_modules/dockview/` match no longer covers the React bindings, so they leaked from `vendor-workbench-dockview` (635→663 kB once fixed) into the canvas chunk (133→105 kB). Build stays green either way — check chunk sizes after any dependency rename.
- 2026-08-26T17:34Z [TOOL] The Shell `working_directory` parameter was silently ignored in this session (cwd stayed at the repo root), so a worktree-targeted `bun add` landed in the main checkout instead. Reverted cleanly, but: verify with `pwd` or use an explicit `cd` in the command when operating inside `.worktrees/*`.
- 2026-08-26T17:34Z [CODE] `useWorkbenchPanelActivityMode` kept on v7 — `dockview-react` ships no hook equivalent; panel props still expose only `api`, so the hook remains the bridge from `onDidActiveChange`/`onDidVisibilityChange` to React state. Re-evaluate on v8, not v7.
- 2026-08-26T17:34Z [CODE] dockview 8.2.0 is already published; #75 deliberately targets 7.x. The 7→8 delta is unreviewed and needs its own pass.
- 2026-08-26T17:00Z [TOOL] dockview-enterprise owns: pinned/multi-row tabs, auto-hide/dock-to-edge, DnD compass, smart guides, layout undo/redo, tab/chip context menus, advanced overflow. Tab groups + edge groups remain free OSS.

- 2026-08-27T21:15Z [TOOL] Swarm item (2/4) shipped: [PR #95](https://github.com/Cozea/electron-app/pull/95) `fix/collab-sync-indicator-states` @ bef81fc8 (worktree `.worktrees/collab-sync-indicator`). Surfaces collab session/encryption on sync context; bounded reconnect indicator + branch menu copy. typecheck + oxlint green.

## [OUTCOMES]
- 2026-08-26T23:02Z [USER] Pulled origin/main (fast-forward 34871a0f → a4246f19, PR #99 substrate cutover). Stash-pop conflicts in `useProjectLaneState.ts` and `ProjectWorkbenchSurface.tsx` resolved: kept lane prefetch + git remote publish; kept keep-alive host + command palette.
- 2026-08-27T22:10Z [CODE] Implementation review found + fixed 2 bugs in the keep-alive work: (a) hidden workbenches left native Electron surfaces painting — browser/dev-server tiles derive `visible` from dockview `panelApi.isVisible`, which can't see CSS hiding; fixed by `surfaceVisible` on `WorkbenchDockRuntimeContext` (set from keep-alive `isActive`), gated centrally in `useWorkbenchPanelActivityMode`. (b) `ProjectLayout` swapped element types between `ActiveWorkspaceProvider`-wrapped and bare `ProjectSyncProvider` branches, remounting the whole shell (and killing all kept-alive sessions) on every cold switch; fixed by always rendering `ActiveWorkspaceContext.Provider` with a nullable memoized value (context default was already null; `useActiveWorkspaceOrNull` exists). typecheck + oxlint + 9 unit tests green.
- 2026-08-27T21:20Z [TOOL] Swarm item 1 shipped: [PR #94](https://github.com/Cozea/electron-app/pull/94) `fix/workbench-branch-git-honesty` @ 52f9764e (worktree `.worktrees/branch-git-honesty`). Branch pill honesty: no-git label, ahead/behind (no remote), unverified `?` on stale git status. typecheck + oxlint green.
- 2026-08-26T17:51Z [USER]+[TOOL] Merged [#77](https://github.com/Cozea/electron-app/pull/77) free dockview OSS policies → main @ `44e4bbf9`.
- 2026-08-26T17:31Z [USER]+[TOOL] Swarm upgrade PRs merged to main: #71 → #72 → #73 → #75 → #76 (merge order on main: 71,72,73,75,76). Head was `197c7d25`.
- Left open (unrelated): draft #74 docs t3code upgrade path — not part of this swarm.
- Hold: Vite 8, TS 7, dockview 8 / enterprise without [USER] approval.

- 2026-08-27T21:30Z [USER]+[TOOL] Branch/sync honesty swarm merged to main: #97 → #96 → #94 (rebased) → #95 (rebased). Head `34871a0f`.
