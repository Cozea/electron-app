name: PR141 main-port final fixes

on:
  push:
    branches:
      - integration/pr141-main-port
    paths:
      - scripts/pr141-main-port-fixes.py
      - .github/workflows/pr141-main-port.yml
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: pr141-main-port-fixes-${{ github.ref }}
  cancel-in-progress: false

jobs:
  fix:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: false
      - name: Apply final current-main compatibility fixes
        run: python3 scripts/pr141-main-port-fixes.py
      - name: Verify patch shape
        run: |
          git diff --check
          git status --short
      - name: Commit fixes
        run: |
          if git diff --quiet; then
            echo 'No changes to commit.'
            exit 0
          fi
          git config user.name 'Cozea collaboration port'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add --all
          git commit -m 'fix(collaboration): finish current-main runtime adaptation'
          git push origin HEAD:integration/pr141-main-port
