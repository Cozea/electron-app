export const AI_MODEL_SELECTOR_CONFIG = {
  // When false, model selectors only show the current provider's models.
  // Flip to true to re-enable cross-provider switching.
  allowCrossProviderSwitching: false,
} as const
