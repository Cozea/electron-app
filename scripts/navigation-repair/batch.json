{
  "operations": [
    {"kind":"replace","path":"apps/desktop/src/lib/workbenchScopeKey.ts","search":"import {\n  buildLegacyWorkspaceIdentityKey,","replacement":"import { getWorkspaceBindingRevision } from './workspaceBindingState'\n\nimport {\n  buildLegacyWorkspaceIdentityKey,"},
    {"kind":"replace","path":"apps/desktop/src/lib/workbenchScopeKey.ts","search":"  workspaceId?: string | null,\n): string {","replacement":"  workspaceId?: string | null,\n  workspaceRevision?: number,\n): string {"},
    {"kind":"replace","path":"apps/desktop/src/lib/workbenchScopeKey.ts","search":"  return buildWorkspaceIdentityKey(projectId, normalizedWorkspace, normalizeLaneId(laneId))!","replacement":"  return buildWorkspaceIdentityKey(projectId, normalizedWorkspace, normalizeLaneId(laneId),\n    workspaceRevision ?? getWorkspaceBindingRevision(normalizedWorkspace) ?? 1)!"},
    {"kind":"replace","path":"apps/desktop/src/lib/workbenchStore.ts","search":"import { createJSONStorage, persist, type StateStorage } from \"zustand/middleware\"","replacement":"import { assertWorkbenchMutationIdentity, getWorkspaceBindingRevision, isWorkspaceBindingCurrent } from './workspaceBindingState'\nimport {\n  ensureWorkbenchModelReady, isWorkbenchModelReady, registerWorkbenchModelAdapter,\n  persistChangedWorkbenchModels, hydrateWorkbenchModelsForProject, reportWorkbenchPersistenceError,\n} from './workbenchPersistence'\nimport { flushWorkbenchStorage } from './workbenchPersistence'\nexport { flushWorkbenchStorage, ensureWorkbenchModelReady } from './workbenchPersistence'"},
    {"kind":"replaceRegion","path":"apps/desktop/src/lib/workbenchStore.ts","start":"const PERSIST_DEBOUNCE_MS = 500","end":"/**\n * Tile shapes live in `@/lib/workbenchTileContract`","replacement":""},
    {"kind":"replace","path":"apps/desktop/src/lib/workbenchStore.ts","search":"export interface WorkbenchProjectState {\n  projectId: string","replacement":"export interface WorkbenchProjectState {\n  /** Older saved records omit this and belong to their existing v1 scope only. */\n  workspaceRevision?: number\n  projectId: string"},
    {"kind":"replace","path":"apps/desktop/src/lib/workbenchStore.ts","search":"    projectId: workbench.projectId,\n    laneId: normalizeLaneId(workbench.laneId),","replacement":"    projectId: workbench.projectId,\n    workspaceRevision: workbench.workspaceRevision,\n    laneId: normalizeLaneId(workbench.laneId),"},
    {"kind":"replace","path":"apps/desktop/src/lib/workbenchStore.ts","search":"export const useProjectWorkbenchStore = create<ProjectWorkbenchState>()(\n  persist(\n    immer((set) => ({","replacement":"export const useProjectWorkbenchStore = create<ProjectWorkbenchState>()(\n    immer((set) => ({"},
    {"kind":"replaceRegion","path":"apps/desktop/src/lib/workbenchStore.ts","start":"    {\n      name: \"cozea:project-workbench\",","end":"if (import.meta.env.DEV && typeof window !== \"undefined\") {","replacement":")\n\nregisterWorkbenchModelAdapter({\n  get: (scopeKey) => useProjectWorkbenchStore.getState().workbenches[scopeKey] ?? null,\n  install: (scopeKey, model) => useProjectWorkbenchStore.setState((state) => ({\n    workbenches: { ...state.workbenches, [scopeKey]: model },\n  })),\n  sanitize: sanitizeWorkbenchState,\n})\n\nuseProjectWorkbenchStore.subscribe((state, previous) => {\n  if (state.workbenches === previous.workbenches) return\n  persistChangedWorkbenchModels(state.workbenches, previous.workbenches)\n})\n\nif (false) { /* The legacy localStorage persistence owner was removed. */ }\n\n"},
    {"kind":"replace","path":"apps/desktop/src/lib/workbenchStore.ts","search":"  const scopeKey = normalizedWorkspace\n    ? promoteLegacyWorkbenchIfNeeded(workbenches, projectId, normalizedLaneId, normalizedWorkspace)\n    : resolveWorkbenchScopeKey(workbenches, projectId, normalizedLaneId, null)","replacement":"  let bindingRevision: number | null = null\n  if (typeof window !== 'undefined') {\n    if (!normalizedWorkspace) throw new Error('Workbench mutations require a resolved workspace.')\n    bindingRevision = assertWorkbenchMutationIdentity(projectId, normalizedLaneId, normalizedWorkspace)\n    if (!isWorkbenchModelReady(projectId, normalizedLaneId, normalizedWorkspace)) {\n      throw new Error('Saved workbench state is still loading. No empty replacement was created.')\n    }\n  }\n  const scopeKey = normalizedWorkspace\n    ? (typeof window === 'undefined'\n      ? promoteLegacyWorkbenchIfNeeded(workbenches, projectId, normalizedLaneId, normalizedWorkspace)\n      : buildWorkbenchScopeKey(projectId, normalizedLaneId, normalizedWorkspace, bindingRevision ?? undefined))\n    : resolveWorkbenchScopeKey(workbenches, projectId, normalizedLaneId, null)"},
    {"kind":"replace","path":"apps/desktop/src/lib/workbenchStore.ts","search":"    workbench = createDefaultWorkbenchState(projectId, normalizedLaneId, normalizedWorkspace)\n    workbenches[scopeKey] = workbench","replacement":"    workbench = createDefaultWorkbenchState(projectId, normalizedLaneId, normalizedWorkspace)\n    if (bindingRevision) workbench.workspaceRevision = bindingRevision\n    workbenches[scopeKey] = workbench"},
    {"kind":"replace","path":"apps/desktop/src/lib/workbenchStore.ts","search":"  if (workbench) {\n    workbench.projectId = projectId","replacement":"  if (workbench) {\n    if (bindingRevision && (workbench.workspaceRevision ?? 1) !== bindingRevision) throw new Error('Stale workbench binding revision.')\n    if (bindingRevision) workbench.workspaceRevision = bindingRevision\n    workbench.projectId = projectId"},
    {"kind":"replace","path":"apps/desktop/src/lib/workbenchStore.ts","search":"        ensureWorkbench: (projectId, laneId, workspaceId) => {\n          if (!projectId) return","replacement":"        ensureWorkbench: (projectId, laneId, workspaceId) => {\n          if (!projectId) return\n          if (typeof window !== 'undefined' && !isWorkbenchModelReady(projectId, normalizeLaneId(laneId), workspaceId ?? null)) {\n            const revision = getWorkspaceBindingRevision(workspaceId)\n            if (!workspaceId || !revision) return\n            const identity = { projectId, laneId: normalizeLaneId(laneId), workspaceId, workspaceRevision: revision }\n            void ensureWorkbenchModelReady(identity).then(() => {\n              if (isWorkspaceBindingCurrent(identity)) useProjectWorkbenchStore.getState().actions.ensureWorkbench(projectId, laneId, workspaceId)\n            }).catch(reportWorkbenchPersistenceError)\n            return\n          }"},
    {"kind":"replace","path":"apps/desktop/src/lib/workbenchStore.ts","search":"          flushWorkbenchStorage()","replacement":"          void flushWorkbenchStorage().catch(reportWorkbenchPersistenceError)"},
    {"kind":"replace","path":"apps/desktop/src/lib/workbenchStore.ts","search":"        cloneWorkspaceState: (projectId, fromWorkspace, toWorkspace) => {","replacement":"        cloneWorkspaceState: async (projectId, fromWorkspace, toWorkspace) => {"},
    {"kind":"replace","path":"apps/desktop/src/lib/workbenchStore.ts","search":"          set((state) => {\n            const normalizedSourceWorkspace = normalizeWorkspaceId(fromWorkspace)","replacement":"          if (typeof window !== 'undefined') {\n            await hydrateWorkbenchModelsForProject(projectId)\n            const revision = getWorkspaceBindingRevision(normalizedTargetWorkspace)\n            if (!revision) throw new Error('The relink target workspace has not been validated.')\n            const sourceModels = Object.values(useProjectWorkbenchStore.getState().workbenches).filter((model) =>\n              model.projectId === projectId && (!fromWorkspace || model.workspaceId === fromWorkspace))\n            await Promise.all(sourceModels.map((model) => ensureWorkbenchModelReady({\n              projectId, workspaceId: normalizedTargetWorkspace, workspaceRevision: revision, laneId: model.laneId,\n            })))\n          }\n          set((state) => {\n            const normalizedSourceWorkspace = normalizeWorkspaceId(fromWorkspace)"},
    {"kind":"replace","path":"apps/desktop/src/lib/workbenchStore.ts","search":"              state.workbenches[targetScopeKey] = cloneWorkbenchState(\n                workbench,\n                normalizedTargetWorkspace,\n              )","replacement":"              state.workbenches[targetScopeKey] = { ...cloneWorkbenchState(workbench, normalizedTargetWorkspace),\n                workspaceRevision: getWorkspaceBindingRevision(normalizedTargetWorkspace) ?? 1 }"},
    {"kind":"replace","path":"apps/desktop/src/app/resources/workspaceResources.ts","search":"import { workspaceCatalogMirror } from './workspaceCatalogMirror';","replacement":"import { workspaceCatalogMirror } from './workspaceCatalogMirror';\nimport { publishWorkspaceBinding, invalidateWorkspaceBinding } from '@/lib/workspaceBindingState';"},
    {"kind":"replace","path":"apps/desktop/src/app/resources/workspaceResources.ts","search":"          this.bindings.set(result.workspace.workspaceId, { projectId: request.projectId, revision: result.workspace.workspaceRevision });","replacement":"          this.bindings.set(result.workspace.workspaceId, { projectId: request.projectId, revision: result.workspace.workspaceRevision });\n          publishWorkspaceBinding({ projectId: request.projectId, workspaceId: result.workspace.workspaceId, workspaceRevision: result.workspace.workspaceRevision });"},
    {"kind":"replace","path":"apps/desktop/src/app/resources/workspaceResources.ts","search":"        this.bindings.set(entry.workspace.workspaceId, { projectId: entry.projectId, revision: entry.workspace.workspaceRevision });","replacement":"        this.bindings.set(entry.workspace.workspaceId, { projectId: entry.projectId, revision: entry.workspace.workspaceRevision });\n        publishWorkspaceBinding({ projectId: entry.projectId, workspaceId: entry.workspace.workspaceId, workspaceRevision: entry.workspace.workspaceRevision });"},
    {"kind":"replace","path":"apps/desktop/src/app/resources/workspaceResources.ts","search":"      if (old && (!current || current.workspace.workspaceId !== old.workspace.workspaceId || current.status !== 'ready')) this.bindings.delete(old.workspace.workspaceId);","replacement":"      if (old && (!current || current.workspace.workspaceId !== old.workspace.workspaceId || current.status !== 'ready')) {\n        this.bindings.delete(old.workspace.workspaceId);\n        invalidateWorkspaceBinding(old.workspace.workspaceId);\n      }"}
  ]
}
