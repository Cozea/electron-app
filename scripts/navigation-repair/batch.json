{
  "operations": [
    {"kind":"replace","path":"apps/desktop/src/app/model/persistence/persistenceMirror.ts","search":"  constructor(private readonly options: MirrorOptions) {}","replacement":"  private readonly options: MirrorOptions;\n  constructor(options: MirrorOptions) { this.options = options; }"},
    {"kind":"replace","path":"apps/desktop/src/app/model/persistence/persistenceWriteQueue.ts","search":"  constructor(private readonly options: QueueOptions) {}","replacement":"  private readonly options: QueueOptions;\n  constructor(options: QueueOptions) { this.options = options; }"},
    {"kind":"replace","path":"apps/desktop/src/app/resources/keyedResource.ts","search":"  constructor(private readonly options: KeyedResourceOptions<T>) { this.key = options.key; }","replacement":"  private readonly options: KeyedResourceOptions<T>;\n  constructor(options: KeyedResourceOptions<T>) { this.options = options; this.key = options.key; }"},
    {"kind":"replace","path":"apps/desktop/src/app/resources/resourcePool.ts","search":"  constructor(private readonly options: { maxIdle?: number; idleTtlMs?: number; onDemandChange?: () => void } = {}) {}","replacement":"  private readonly options: { maxIdle?: number; idleTtlMs?: number; onDemandChange?: () => void };\n  constructor(options: { maxIdle?: number; idleTtlMs?: number; onDemandChange?: () => void } = {}) { this.options = options; }"},
    {"kind":"replace","path":"apps/desktop/src/app/resources/workspaceCatalogMirror.ts","search":"  constructor(private readonly bridge: () => CatalogBridge | undefined) {}","replacement":"  private readonly bridge: () => CatalogBridge | undefined;\n  constructor(bridge: () => CatalogBridge | undefined) { this.bridge = bridge; }"},
    {"kind":"replace","path":"apps/desktop/src/app/resources/workspaceCatalogMirror.ts","search":"  return bridge?.getCatalogSnapshot && bridge.onCatalogSnapshotChanged ? bridge : undefined;","replacement":"  return bridge;"},
    {"kind":"replace","path":"apps/desktop/src/app/resources/workspaceResources.ts","search":"  constructor(private readonly dependencies: WorkspaceResourceDependencies) {","replacement":"  private readonly dependencies: WorkspaceResourceDependencies;\n  constructor(dependencies: WorkspaceResourceDependencies) {\n    this.dependencies = dependencies;"},
    {"kind":"replace","path":"apps/desktop/electron/workers/desktopStatePersistenceWorkerCore.ts","search":"  constructor(private readonly options: WorkerCoreOptions) {","replacement":"  private readonly options: WorkerCoreOptions;\n  constructor(options: WorkerCoreOptions) {\n    this.options = options;"},
    {"kind":"replace","path":"apps/desktop/electron/services/DesktopStatePersistenceService.ts","search":"  readonly serviceEpoch = randomUUID();","replacement":"  readonly serviceEpoch: string = randomUUID();"},
    {"kind":"replace","path":"tests/navigation/desktopPersistenceClient.test.ts","search":"    flush: vi.fn(async () => ({ status: 'flushed', flushedRevision: watermark, serviceEpoch: 'service' })),","replacement":"    flush: vi.fn(async () => ({ status: 'flushed' as const, flushedRevision: watermark, serviceEpoch: 'service' })),"},
    {"kind":"replace","path":"apps/desktop/src/app/resources/workspaceResources.ts","search":"      projectSlug: request.projectSlug ?? null, expectedRepo: repoKey(request.expectedRepo),","replacement":"      projectSlug: scan ? request.projectSlug ?? null : null, expectedRepo: repoKey(request.expectedRepo),\n      preferredLaneId: request.preferredLaneId ?? null,"},
    {"kind":"replace","path":"apps/desktop/src/app/resources/workspaceResources.ts","search":"      preferredWorkspaceId: input.preferredWorkspaceId ?? null, expectedRepo: input.expectedRepo ?? null, allowCandidateScan: scan };","replacement":"      preferredWorkspaceId: input.preferredWorkspaceId ?? null, preferredLaneId: input.preferredLaneId ?? null,\n      expectedRepo: input.expectedRepo ?? null, allowCandidateScan: scan };"},
    {"kind":"replace","path":"apps/desktop/src/app/resources/workspaceResources.ts","search":"      if (entry.status === 'ready') this.bindings.set(entry.workspace.workspaceId,\n        { projectId: entry.projectId, revision: entry.workspace.workspaceRevision });","replacement":"      const known = this.bindings.get(entry.workspace.workspaceId);\n      if (entry.status === 'ready' && (!known || known.revision <= entry.workspace.workspaceRevision)) {\n        this.bindings.set(entry.workspace.workspaceId, { projectId: entry.projectId, revision: entry.workspace.workspaceRevision });\n      }"},
    {"kind":"replace","path":"apps/desktop/src/app/resources/workspaceResources.ts","search":"    this.git.invalidate((metadata) => !workspaceId || metadata.workspaceId === workspaceId, 'branch changed');","replacement":"    const affected = new Set(this.lanes.values().filter((entry) => entry.metadata.projectId === projectId &&\n      (!workspaceId || entry.metadata.workspaceId === workspaceId)).map((entry) => entry.metadata.workspaceId));\n    this.git.invalidate((metadata) => affected.has(metadata.workspaceId), 'branch changed');"},
    {"kind":"replace","path":"apps/desktop/electron/workers/desktopStatePersistenceWorkerCore.ts","search":"    const entries: Array<{ filePath: string; bytes: number; updatedAt: number }> = [];","replacement":"    const entries: Array<{ filePath: string; bytes: number; updatedAt: number; record: DesktopStateRecord }> = [];"},
    {"kind":"replace","path":"apps/desktop/electron/workers/desktopStatePersistenceWorkerCore.ts","search":"          entries.push({ filePath, bytes: Buffer.byteLength(raw, 'utf8'), updatedAt: record.updatedAt });","replacement":"          if (filePath === this.recordPath(record.namespace, record.key)) {\n            entries.push({ filePath, bytes: Buffer.byteLength(raw, 'utf8'), updatedAt: record.updatedAt, record });\n          }"},
    {"kind":"replace","path":"apps/desktop/electron/workers/desktopStatePersistenceWorkerCore.ts","search":"      if (index >= MAX_PERSISTED_QUERY_ENTRIES || bytes > QUERY_CACHE_PERSISTED_BUDGET) await fs.unlink(entry.filePath);","replacement":"      if (index >= MAX_PERSISTED_QUERY_ENTRIES || bytes > QUERY_CACHE_PERSISTED_BUDGET) {\n        // Keep the acknowledged version so a live renderer's next update does not\n        // conflict after cache eviction. This policy tombstone is not a user edit.\n        await this.replace({ ...entry.record, data: null, deleted: true });\n      }"}
  ]
}
